[package]
name = "worldbend-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false
description = "Canonical projective plane-mapping model, solver, validation, and diagnostics"

[features]
default = ["css", "place", "deform", "surface-deformation", "remap", "timeline", "motion", "program", "template"]
css = []
place = []
deform = []
surface-deformation = ["deform"]
remap = []
timeline = []
motion = ["timeline"]
program = []
template = ["place", "program"]

[dependencies]
nalgebra.workspace = true
schemars.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
approx = "0.5.1"
proptest.workspace = true
